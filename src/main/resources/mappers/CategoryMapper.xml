<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.quiz.mapper.CategoryMapper">

    <select id="findAll" resultType="com.example.quiz.model.Category">
        SELECT id, name FROM categories ORDER BY id
    </select>
    
    <select id="findAllWithQuestionCount" resultType="com.example.quiz.model.CategoryWithCount">
        SELECT 
            c.id,
            c.name,
            COUNT(q.id) AS questionCount
        FROM categories c
        LEFT JOIN questions q ON c.id = q.category_id
        GROUP BY c.id, c.name
        ORDER BY c.id
    </select>

</mapper>